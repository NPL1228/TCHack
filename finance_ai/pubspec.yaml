name: ailedge
description: "AiLedge – AI-Powered Personal Financial Planner & Grocery Spending Recorder"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.1.2

  # AI / Gemini
  google_generative_ai: ^0.4.6

  # Charts
  fl_chart: ^0.69.0

  # Image Picking (camera / gallery)
  image_picker: ^1.1.2

  # Local Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Utilities
  intl: ^0.19.0
  uuid: ^4.5.1
  path_provider: ^2.1.5

  # UI / Animations
  animate_do: ^3.3.4
  font_awesome_flutter: ^10.8.0
  percent_indicator: ^4.2.4
  shimmer: ^3.0.0
  flutter_staggered_animations: ^1.1.1

  # Google Fonts
  google_fonts: ^6.2.1

  cupertino_icons: ^1.0.8
  flutter_dotenv: ^6.0.0
  http: ^1.6.0
  file_picker: ^10.3.10
  share_plus: ^12.0.1
  excel: ^4.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  build_runner: ^2.4.13
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
